const create = React.createElement(
    "h1",
     {id: "heading" },
     "I will complete react in just 2 days" );
        const root  =ReactDOM.createRoot(document.getElementById("root"));
        root.render(create);